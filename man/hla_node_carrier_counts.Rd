% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_association_core.R
\name{hla_node_carrier_counts}
\alias{hla_node_carrier_counts}
\title{Per-node carrier / non-carrier / untyped counts for one allele}
\usage{
hla_node_carrier_counts(samples_all, typing, samples, allele)
}
\arguments{
\item{samples_all}{Character vector, one entry per node: a comma-separated
sorted sample list (the `samples_all` node attribute).}

\item{typing}{Canonical HLA typing table.}

\item{samples}{In-scope immune-repertoire sample names.}

\item{allele}{Canonical or normalizable HLA allele.}
}
\value{
data.frame(n_carrier, n_noncarrier, n_untyped), one row per node.
}
\description{
The counts behind [hla_node_carrier_status()]. A colour alone cannot say
whether a "Carrier" node rests on ten carriers or on one carrier and nine
untyped samples, so the UI shows these next to the label.
}
\keyword{internal}
