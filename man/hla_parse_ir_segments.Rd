% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_motif_core.R
\name{hla_parse_ir_segments}
\alias{hla_parse_ir_segments}
\title{Parse V / J / CDR3 for one chain out of scRepertoire CT* columns}
\usage{
hla_parse_ir_segments(data, chain)
}
\arguments{
\item{data}{Named list of IR data.frames (with metadata joined by barcode).}

\item{chain}{Chain prefix, e.g. "TRB" / "TRA".}
}
\value{
A data.frame with one row per cell carrying `chain`, or NULL.
}
\description{
scRepertoire packs every chain of a cell into single underscore-joined
strings (`CTgene`, `CTaa`); this returns one row per cell that HAS the
requested chain, with parsed `v_gene` / `j_gene` / `cdr3` plus a combined
`clone_vjc = "v;j;cdr3"` clone identity and every metadata column already
joined onto the IR data.
}
\keyword{internal}
