% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_motif_core.R
\name{hla_build_motif_graph}
\alias{hla_build_motif_graph}
\title{Build the CDR3 Hamming-1 motif igraph from parsed segments}
\usage{
hla_build_motif_graph(
  seg,
  by_v = FALSE,
  min_nodes = 2L,
  show_isolated = FALSE,
  meta_cols = character(0)
)
}
\arguments{
\item{seg}{Output of [hla_parse_ir_segments()].}

\item{by_v}{Split clustering within V gene.}

\item{min_nodes}{Keep connected components of size >= `min_nodes`. Default 2.}

\item{show_isolated}{When TRUE, also keep isolated (degree-0) CDR3s as points.}

\item{meta_cols}{Metadata columns to carry as node distributions.}
}
\value{
An igraph object (with a per-node `cluster` attribute and a
  `total_cells` graph attribute) or NULL. Attaches attr "guard" with a
  message when a size guard tripped (graph is NULL in that case).
}
\description{
Parses nothing itself: takes already-parsed segments, aggregates to unique
CDR3 nodes, clusters by Hamming distance 1 (optionally within V gene), and
returns an igraph whose vertices carry `cdr3` / `motif_*` attributes + per-
node metadata distributions + `clone_count`.
}
\keyword{internal}
