% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createShinyApp.R
\name{createShinyApp}
\alias{createShinyApp}
\title{Create a self-contained Shiny app folder for Cerebro v1.4}
\usage{
createShinyApp(
  cerebro_data,
  result_dir = NULL,
  max_request_size = 8000,
  port = 8080,
  host = "127.0.0.1",
  launch_browser = TRUE,
  quiet = FALSE,
  display_mode = "normal",
  colors = NULL,
  cerebro_options = list(exclude_trivial_metadata = TRUE),
  overwrite = TRUE,
  verbose = TRUE,
  crb_pick_smallest_file = TRUE,
  show_upload_ui = TRUE,
  welcome_message = "Welcome to CerebroNexus!",
  point_size = list(overview_projection_point_size = NULL),
  variable_to_compare = NULL,
  spatial_images = NULL,
  spatial_images_flip_x = NULL,
  spatial_images_flip_y = NULL,
  spatial_images_scale_x = NULL,
  spatial_images_scale_y = NULL,
  spatial_images_offset_x = NULL,
  spatial_images_offset_y = NULL,
  spatial_plot_rotation = NULL,
  ...
)
}
\arguments{
\item{cerebro_data}{Named character vector or list of \code{.crb} (or
\code{.rds}) file paths. Names are used as dataset labels.}

\item{result_dir}{Output directory.}

\item{max_request_size}{Max upload size in MB; defaults to 8000.}

\item{port}{Port the generated app listens on; defaults to 1337.}

\item{host}{Host the generated app binds to; defaults to "127.0.0.1".}

\item{launch_browser}{Whether to launch a browser; defaults to TRUE.}

\item{quiet}{Passed to \code{shiny::runApp}; defaults to FALSE.}

\item{display_mode}{\code{shiny::runApp} display mode; defaults to "normal".}

\item{colors}{Optional named list of colour palettes per dataset.}

\item{cerebro_options}{Extra entries merged into \code{Cerebro.options} in
the generated app.}

\item{overwrite}{If \code{TRUE} (default), wipe \code{result_dir} first.}

\item{verbose}{Print progress messages; defaults to TRUE.}

\item{crb_pick_smallest_file}{Forwarded to \code{Cerebro.options}.}

\item{show_upload_ui}{Forwarded to \code{Cerebro.options}.}

\item{welcome_message}{Welcome message shown in the Load Data tab.}

\item{point_size}{Named list with \code{overview_projection_point_size}
(and optionally other keys) forwarded to \code{Cerebro.options}.}

\item{variable_to_compare}{Forwarded to \code{Cerebro.options}.}

\item{spatial_images}{Named list/vector of paths to spatial background images
(e.g. tissue histology) shown behind the Spatial tab projection. Names must
match \code{cerebro_data}. Images are copied into the app bundle.}

\item{spatial_images_flip_x}{Named list/vector; whether to flip the spatial
background image horizontally. Names must match \code{cerebro_data}.}

\item{spatial_images_flip_y}{Named list/vector; whether to flip the spatial
background image vertically. Names must match \code{cerebro_data}.}

\item{spatial_images_scale_x}{Named list/vector; scaling factor for the X
axis of the spatial background image. Names must match \code{cerebro_data}.}

\item{spatial_images_scale_y}{Named list/vector; scaling factor for the Y
axis of the spatial background image. Names must match \code{cerebro_data}.}

\item{spatial_images_offset_x}{Named list/vector; horizontal offset (in data
units) applied to move the spatial background image. Names must match
\code{cerebro_data}.}

\item{spatial_images_offset_y}{Named list/vector; vertical offset (in data
units) applied to move the spatial background image. Names must match
\code{cerebro_data}.}

\item{spatial_plot_rotation}{Named list/vector; initial rotation (degrees)
applied to spatial cell coordinates. Names must match \code{cerebro_data}.}

\item{...}{Currently unused; reserved for future arguments.}
}
\value{
Invisibly returns \code{result_dir}.
}
\description{
Bundles a Cerebro v1.4 Shiny app into \code{result_dir}, copying the
\code{inst/shiny/v1.4/} sources, the requested \code{.crb} data file(s),
and \code{extdata/}, and writes an \code{app.R} that sources the bundled
UI/server. The output directory can be served directly by shiny-server or
run with \code{shiny::runApp(result_dir)}.
}
\details{
Supports external expression backends (\code{bpcells}, \code{h5}) in
addition to the embedded mode. When \code{cerebro_data} points to a
\code{.crb} with an external backend, the sibling \code{.bpcells/}
directory or \code{.h5} file is detected and copied into the bundle
alongside the \code{.crb}.
}
