% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_motif_core.R
\name{hla_build_motif_groups}
\alias{hla_build_motif_groups}
\title{Build motif groups over all length bins (optionally within V gene)}
\usage{
hla_build_motif_groups(df, by_v = FALSE)
}
\arguments{
\item{df}{data.frame with `cdr3` (+ `v_gene` when `by_v`); one row per
unique node key already aggregated by the caller.}

\item{by_v}{When TRUE, split by (V gene, length) and prefix motif ids by V.}
}
\value{
list(motif_df = per-CDR3 assignment, edges = Hamming==1 pairs or NULL)
}
\description{
Splits unique CDR3s into equal-length bins (Hamming only compares equal
lengths), clusters each, and stitches results back together.
}
\keyword{internal}
